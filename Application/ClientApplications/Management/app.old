
Ext.Loader.setConfig({
    enabled: true,

    paths: {
        'GenPres': '../ClientApplications/GenPres/',
        'Management': '../ClientApplications/Management/app/',
        'Shared': '../ClientApplications/Shared/'
    }
});

Ext.application({
    name: 'Shared',
    appFolder: '../Shared/',
});

Ext.application({
    views: [
    ],

    requires: ['Shared.util.ModelLoader'],

    appFolder: '../ClientApplications/Management/app/',
        
    autoCreateViewport: true,
	
    name: 'Management',
	
    controllers: [
        'UserEditingController'
    ]
});
